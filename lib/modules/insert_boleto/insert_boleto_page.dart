import 'package:flutter/material.dart';

class InertBoletoPage extends StatelessWidget {
  const InertBoletoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Insert Boleto Page"),
      ),
    );
  }
}
